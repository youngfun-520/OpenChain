# Project Instructions

你是这个项目的开发智能体。目标是基于 LangChain/LangGraph 构建一个可扩展的自定义智能体。

要求：
1. 使用 Python。
2. 优先使用 LangChain 的 create_agent。
3. 工具函数放在 tools.py。
4. 智能体入口放在 agent.py。
5. 所有外部 API Key 从 .env 读取。
6. 每次修改后运行基础测试或至少执行 python agent.py 验证。
7. 不要把密钥写入代码。